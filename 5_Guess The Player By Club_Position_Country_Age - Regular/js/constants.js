/**
 * Play Video: question → question — exit phase duration.
 * Must match `.stage-exit-video-anim` / panel exit duration in `css/components/transitions.css`.
 */
export const STAGE_VIDEO_LEVEL_TRANSITION_MS = 820;

/**
 * Play Video: enter phase (new level fades in).
 * Must match `.stage-enter-video-anim` / `.stage-enter-video-anim-panel` in `transitions.css`.
 */
export const STAGE_VIDEO_LEVEL_ENTER_MS = 820;

/** Gauge color segments from easy (green) to hard (red) */
export const gaugeColors = [
  "#22c55e",
  "#4ade80",
  "#a3e635",
  "#eab308",
  "#facc15",
  "#f59e0b",
  "#ea580c",
  "#f97316",
  "#ef4444",
  "#b91c1c",
];
