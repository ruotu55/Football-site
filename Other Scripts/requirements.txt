# Transfermarkt data via tmapi (used by generate_squads_from_transfermarkt.py)
transfermarkt-wrapper==0.0.8
certifi>=2024.0.0
aiohttp>=3.9.0

# Background removal (remove_backgrounds.py) — rembg needs [cpu] or [gpu] for onnxruntime
pillow==12.1.1
rembg[cpu]==2.0.74
