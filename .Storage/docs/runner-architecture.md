# Runner App Architecture

How a runner app is built, what each JS module does, and the shared modules.

## The runners (quiz types)
| # | Quiz | Variants |
|---|------|----------|
| 1 | Guess the Team Name (club, by player nationality) | **Regular = PREP PANEL (Remotion)** — see below, Shorts, _Remotion |
| 2 | Guess the National Team (by player club) | **Regular = PREP PANEL (Remotion)** — same as runner 1, see below; Shorts, _Remotion |
| 3 | Guess the Player by Career Path | **Regular = PREP PANEL (Remotion)** — see below (custom career-path layout); Shorts, _Remotion |
| 4 | Guess the Player by Career Stats | **Regular = PREP PANEL (Remotion)** — see below (career-stats layout); Shorts, _Remotion (custom `Level.tsx`: stat boxes + waving flag + reveal) |
| 5 | Guess the Player by Club/Position/Country/Age ("four params") | Regular, Shorts |
| 6 | Guess the Fake Information | Regular, Shorts |
| 7 | Guess the Team Logo Name | Regular, Shorts |
| 8 | Guess the Player Name | Regular, Shorts |
| 9 | Football Quiz Multiple Choice (A/B/C) | Regular only |

**Naming gotchas:** folders 4/5/6 lack "Main Runner" in the name (glob `1..9`, not `*Main Runner*`). Folder 6 has the literal typo "Informaiton". Runner 3 vs 4 Shorts look alike — confirm the URL/title in the browser before bumping caches. Runner 9 (MCQ) was cloned from #6; uses `mcq-mode.js`.

> **Every Regular runner (1–9) also has a Remotion project** that re-creates its play-video look, all grouped under the top-level **`___Remotion___/`** folder on a shared library. See **[Remotion projects — all Regular runners](#remotion-projects--all-regular-runners-_remotion_)** below.

## Boot sequence
`index.html` loads shared scripts (debug-overlay, loading-overlay, modal-layer, calendar schedule + recording-status-client), then an inline module fetches `html/*.html` partials into the DOM (`controls, progress, modals, pitch, landing, logo, outro`), moves the team header into its mount, and finally injects `js/app.js?v=<Date.now()>`. `app.js` builds `appState`, wires UI, imports the subsystems. (**Runner 1 Regular** loads only `controls, modals, pitch` and boots into the prep panel — see its section below.)

## Runners 1 + 2 Regular = Remotion prep panels (2026-06-12)

`1_Guess The Football Team Name - Main Runner - Regular` is no longer a playable quiz — it is the **control panel for preparing Remotion videos**. **Runner 2 Regular (`2_…National Team…`) got the IDENTICAL transformation** (same save-picker `2|` blocks, prep-panel, slim levels/transitions, deletions, auto build-data hook in its `run_site.py` → `___Remotion___/2_…_Remotion`, `?v=20260612-prep2` token). Runner-2 specifics: cards' **front face = the player's CLUB CREST** (`slot-front--national-crest`, "Revealed" OFF shows club logos), the per-level team panel shows the **national-team logo** + flag derived from the SQUAD NAME (the country) via flagcodes, renames resolve via quiz type `nat-by-club` → `team_name_overrides_shared_national.json`, and runner 2's Remotion `build-data.mjs` APPLIES `player_name_overrides_shared.json` + the national bucket + per-save `headerTeamNameOverride`. The whole browser play/record/headless-render layer was deleted (git history keeps it): `video.js`, `levels.js` play flow, `progress.js`, `transitions.js` overlays, `obs-recorder.js`, `recording-flow/preflight/queue.js`, all `render-*.js`, `thumbnail-studio.js`, the `render/` Node folder, `html/landing|logo|outro|progress.html`, and the play/record/render FABs. `run_site.py` (all endpoints) is untouched — open it the same way as before.

**How it works:**
- **Saved tab = `js/save-picker.js`** — lists EXACTLY the saves Remotion renders: named blocks with key prefix `1|` from `/__recording-status` (same filter as `___Remotion___/1_…_Remotion/scripts/build-data.mjs`). Clicking one loads via the old flow: frozen Save Video Status snapshot first, else rebuild from `teamsImportText` + Save Team layouts (`buildScriptFromImportText` → `applyScriptObject`). **Order:** `listedSaves()` sorts **ascending by episode** (`a.episode - b.episode`, episode = `key.split("|")[2]`) → "… 1" at the TOP, "… 10" at the bottom (was descending; flipped per user across all four prep-panel save-pickers 1–4).
- **Main view = `js/prep-panel.js`** — every question level of the loaded save is a stacked section ("Level N — Team"). Each section body is now a **3-TAB block (Assets · Questions · Answer)** (`buildLevelTabs`, copied from runner 4's scaffold; default tab = Assets, switch just toggles `hidden`). It reuses `pitch-render.js`'s `renderSlot`/slot controls UNCHANGED via a context swap: each section owns a `.pitch-slots` clone (now inside the **Assets** pane's `.prep-assets-edit`), and a capture-phase `pointerdown` sets `appState.currentLevelIndex` + `appState.els.pitchSlots` to that section before any click handler runs (all handlers read the "current" level via `getState()` at click time). `appState.videoRevealPostTimerActive = true` is forced so videoMode saves render the revealed card (photo + name + PHOTO/X/CROP controls). The single `#team-header` is the active-level toolbar (crest zoom/nudge/swap/Get-logo + dblclick name edit). Layout in `css/components/prep-panel.css` (flattens the pitch-formation absolute positioning into a grid).
  - **Assets tab** = the EXISTING editable lineup pitch + team-panel editor (Save Team / LOGO+X / Rename) PLUS small asset cards (`buildAssetCardsRow`). All editing is unchanged — the real editable DOM lives in this pane. **(Runner 1: the team-panel box was removed and its LOGO+X / Rename controls relocated; the asset cards were trimmed to just "Logo" + "Flag" — see the runner-1 notes below.)**
    - **Lineup layout (runner 1, 2026-06-14):** `.prep-section .pitch-slots` is a **single row of 11 columns** (`grid-template-columns: repeat(11, 1fr)`). Each `.player-slot` STACKS BOTH slot faces vertically (CSS in `prep-panel.css`, NOT a JS change): `.slot-mount`/`.slot-inner`/`.slot-face` are forced `position:static` and `.slot-inner` becomes a flex column → the `.slot-front` nationality-FLAG circle (3.2rem) sits on TOP of the `.slot-back` photo+red-name card (aspect 10/13.5). The base `.player-slot` `aspect-ratio`/`container-type:size` are overridden to `auto`/`normal` so the slot grows to fit both faces. The old `#prep-root.prep-revealed` one-face toggle is replaced — both faces always show. To make 11 columns fit, `.prep-assets` is now a COLUMN (was a row) so the team panel sits BELOW the full-width pitch instead of beside it. **Controls moved BELOW each card (2026-06-17):** `.slot-photo-controls` is now `top: calc(100% + 0.45rem)` (was `bottom: calc(100% + …)` above the card), `width:100%`, big buttons (`flex: 0 0 calc(33.333% - 0.2rem)`, `min-height:2.4rem`, Barlow Condensed `0.92rem`) so `flex-wrap`+`justify-content:center` lays them out as **"PHOTO X CROP"** on row 1 and **"SWAP NAME"** centred on row 2 (DOM order from `pitch-render.js`: photo·delete·crop·swap·name). The grid swapped `padding-top:4.2rem`→`padding-top:0.8rem` + `padding-bottom:6rem` to clear the now-below controls. **Name size = Remotion (2026-06-17):** the red-band `.slot-name` font (`13.2cqh` in pitch.css) lost its container when the flatten forced `.player-slot{container-type:normal}`, so it had been pinned to `0.85rem`; now `.prep-section .slot-back` gets `container-type:size` and the pin is removed → the name scales with the CARD exactly like the rendered video. Edit handlers/context-swap are untouched (CSS-only). Token `css/styles.css?v=20260617-prepctrls`.
    - **Team-panel box removed (runner 1, 2026-06-14):** the tall right-side "team logo name" box (`buildTeamPanelPreview` / `.prep-assets-side`, a scaled Remotion RevealPanel showing crest → name → flag) was DELETED from runner 1's prep panel (function + `fitTeamPanels` + `RP_W/RP_H` + the resize listener all gone; `.prep-assets` is back to a plain column with only `.prep-assets-main`). Its controls were relocated, not lost: the **LOGO + X** crest download/remove buttons now sit ABOVE the crest image inside the **Team-crest asset card** (`buildCrestLogoControls` → `.prep-asset-crest-wrap`/`.prep-asset-crest-logo-row`, reusing the `.prep-team-panel__logo-btn`/`__logo-del-btn` styles), and **✎ Rename team** moved into the **section head** next to the "Level N · Team" title (`buildRenameBtn`, appended in `fillHead` before the Save Level button which `margin-left:auto` keeps on the right). (Runner 2 still HAS its team-panel box — this was runner-1 only, per the runner-scope rule.)
    - **Asset cards trimmed (runner 1, 2026-06-17):** `buildAssetCardsRow` now renders only **TWO** cards — **"Logo"** (was "Team crest", carries the LOGO+X controls above the crest) and **"Flag"** (was "Flag — <country>", now always just "Flag"). The **DATA card was REMOVED** (Team/Country/Players summary) along with the rename handler's now-dead `.prep-asset-data__v[data-k="Team"]` sync (the `.prep-asset-data*` CSS is left as harmless dead rules). The two card titles (`.prep-asset-card__title`) are doubled to **`1.7rem`** (was 0.85rem). CSS token `css/styles.css?v=20260617-assetcards`.
    - **Lineup layout (runner 2, 2026-06-14):** SAME 11-column stacked-faces treatment applied to `2_…National Team…Regular/css/components/prep-panel.css` (CSS-only). Difference vs runner 1: runner-2 slot FRONTS are **club crests** (`.slot-front--national-crest`), so the top of each column is the club crest (~3.2rem) and below it the player photo + red name band (`.slot-back`). **(2026-06-17: the back card was made IDENTICAL to runner 1 — `aspect-ratio:10/13.5` + the proportional 18% name band (pitch.css default) + `container-type:size` so the `13.2cqh` name scales like runner 1; was `10/12` with a fixed `1.5rem` band, which made the cards/photos/text look different.)** **(2026-06-17b: runner-2 `renderSlot` also sets `img.style.borderRadius="50%"` on the BACK photo — legacy circular-avatar styling — which on the rectangular prep card rendered the photo as an ELLIPSE with white corners ("smaller/upper"). A CSS override (`.prep-section .slot-back .slot-img{border-radius:0}`) fixed the slot-inner path but the photo REVERTED on re-render because `renderSlot` sets `border-radius:50%` on the back photo in TWO paths (the stacked-faces `.slot-back` path AND the no-`.slot-inner` path whose img is NOT inside `.slot-back`, so the CSS missed it). The img `border-radius` was a red herring — the REAL cause was `applyStrictAvatarBounds(backAvatar)`: runner 2's version forced the avatar to `aspect-ratio:1/1` + `border-radius:50%` + `overflow:hidden` = a CIRCLE that clipped the photo (runner 1's `applyStrictAvatarBounds` is a NO-OP that STRIPS those inline styles). ROOT FIX: replaced runner-2's `applyStrictAvatarBounds` body with runner 1's no-op (removeProperty display/width/height/aspect-ratio/border-radius/overflow) so the avatar uses the rectangular trading-card CSS and the photo fills it. Also removed the two back-photo `img.style.borderRadius="50%"` lines. Token `?v=20260617-nocircle`.)** **(2026-06-17c — BOTH runners: the editable Assets card was made a 1:1 ratio match of the Remotion player card so a long name overflows at the SAME point as the video. Changed `.slot-back` to `aspect-ratio:10/12` + `--slot-name-band-ratio:28%` (was 10/13.5 + 18% — a different name-to-card ratio than PlayerSlot.tsx/the Answer frame), and aligned `.slot-name` TEXT metrics to the Remotion band: `font-size:13.3cqh` (=30px), `font-style:normal` (pitch.css default was ITALIC), `letter-spacing:0` (was 0.03em), `padding:0 2%`. Token `?v=20260617-nameratio`.)** BOTH control rows show (both faces visible): `.slot-photo-controls` at `top:0` (PHOTO/X/CROP/SWAP/NAME) and `.slot-logo-controls` at `top:3.4rem` (LOGO/X for the crest) — the old `prep-revealed` reveal-state swap that hid one row is removed; both rows `flex-wrap`+`width:100%` and cubes shrank to 1.5rem (0.42rem font, delete 0.72rem) so they wrap into 2 rows inside the narrow column with no neighbour overlap; grid `padding-top:5.4rem`. `.prep-assets` is COLUMN (team panel below the full-width pitch). Both logo-controls + photo-controls are re-parented onto the `.player-slot` root by `requestAnimationFrame` in `pitch-render.js` (`appendLogoControls`/`appendAutoPhotoFetchButton`), so the absolute `top:` offsets stack correctly. Token `css/styles.css?v=20260617-assets11col`.
    - **Runner-1 changes ported to runner 2 (2026-06-17, token `?v=20260617-prep2port`):** the full set of runner-1 prep edits was replicated into runner 2, adapted for national teams (data from `teamsImportText` lines + national squads/layouts; the "team" is the COUNTRY). Specifically: **(1)** the right-side team-panel box (`buildTeamPanelPreview`/`.prep-assets-side`/`fitTeamPanels`/`RP_W/RP_H`/resize listener) was DELETED; **✎ Rename team** moved into the section head via `buildRenameBtn` in `fillHead` (`.prep-rename-team`). Runner 2's team panel had NO LOGO/X (national logos aren't fetched per-card), so nothing to relocate there. **(2)** Asset cards trimmed to **"Logo"** (was "National team logo") + **"Flag"** (was "Flag — <country>"); the **DATA card removed**; titles doubled to `1.7rem`. **(3)** Controls moved BELOW each card: `.slot-photo-controls` at `top:calc(100% + 0.45rem)` as "PHOTO X CROP / SWAP NAME" (big `flex:0 0 33%`, `min-height:2.4rem`, `0.92rem`); the per-player **club-crest** `.slot-logo-controls` (LOGO/X, reuses the same cube classes) sits ABOVE the card (by the crest it edits) at `bottom:calc(100% + 0.45rem)`; grid `padding-top:3.4rem` (for the LOGO/X row above) + `padding-bottom:6rem` (for the photo block below). (Token `?v=20260617-logotop`.) **(4)** Name size = Remotion: `.slot-back { container-type: size }`. **(5)** Name COLOUR black everywhere: runner 2's `Level.tsx` imported the SHARED `PlayerSlot`, so a runner-2 **LOCAL copy** `src/components/PlayerSlot.tsx` (black band `#111111`, `textShadow:none`; constants/type re-imported from `@shared`) was created and `Level.tsx` repointed to `../components/PlayerSlot` — runners 3–9 (still on `@shared`) are untouched; the `previewPlayerCard` band also went black. **(6)** Video honors per-save `customNames`: runner 2 `build-data.mjs` builds players from saved layouts/squads, so a `perSaveCustomNames` map (country→customNames, from `block.script.levels`) is passed into `toPlayer` → `display = customNames[p.name] || playerDisplay(p.name)` (verified `Neil El Aynaoui → EL AYNAOUI`). Runner 2's preview already used `pitchSlotDisplayLabel` (name parity was pre-existing).
    - **LOGO/X float ABOVE the Logo card (both runners, 2026-06-17):** the LOGO + X buttons no longer sit INSIDE the Logo box (which changed its height) — `.prep-asset-crest-logo-row` is now `position:absolute; bottom:calc(100% + 0.4rem)` relative to the `.prep-asset-card--logo` box (which is `position:relative`), so they float just above it and the box size is fixed (title + 8rem body). `.prep-assets-row` got `margin-top:2.6rem` to clear them. Runner 1 (token `?v=20260617-logoabove`) and runner 2 (token `?v=20260617-logoabove2`) now have an IDENTICAL Logo box (`.prep-asset-card` 15rem wide, 8rem body, 1.7rem title). **Runner 2 also GAINED functional LOGO/X** (it previously had none): `buildLogoControls`/`chooseLogoSource` in runner-2 `prep-panel.js` POST to `/__team-logo/fetch` + `/delete` with `currentSquadImagePath = Images/National Team Logos/<name>.png` (via `nationalLogoRelPath`) so the download/delete targets the NATIONAL-team logo file; refresh is in-place via `bumpAssetCacheBust` + `projectAssetUrlFresh`.
    - **Preview pitch fixed to match the video (runner 2, 2026-06-17):** the Questions/Answer faithful frame painted a SOLID GREEN grass field (`previewPitchSurface` with `repeating-linear-gradient` + green `linear-gradient`), but the real video's `@shared/components/Pitch` is a **TRANSPARENT** surface with only white SVG lines (the themed background shows through). The preview looked "wayyy different". Fixed: `previewPitchSurface()` now returns the same white-line SVG (viewBox 160×100) as runner 1's faithful preview, over the parent surface's dark gradient — so the preview pitch == the rendered pitch. The surface div styling + `crestImgPct={72}` already matched Level.tsx. Token `?v=20260617-pitchfix`.
    - **"Get all photos" + clear "X" centred & sticky (both runners, 2026-06-17):** both buttons are re-parented to `<body>` elsewhere (`installBulkPhotoButton` in pitch-render + the clear-team setup in app.js). `prep-panel.js` `dockTopActions()` (called via `requestAnimationFrame` from `renderPrepPanel`/`initPrepPanel`) groups them into ONE fixed, centred wrapper `#prep-top-actions` (`position:fixed; top:0.7rem; left:50%; transform:translateX(-50%); display:flex; gap:0.5rem; z-index:40`) so the PAIR's bounding box is centred above the tab bar — centring each button SEPARATELY (the first attempt: `right/left:calc(50% ± 0.3rem)`) left the wide "Get all photos" looking left-heavy, and the gap was too small. The sticky `.prep-globaltabbar` is `top:4.2rem` (clearly below the buttons) and `#prep-root` `padding-top:4.8rem` aligns the bar under them with no gap when unscrolled. The two buttons are `position:static` inside the wrapper. Token `?v=20260617-stickytop2`.
  - **Questions / Answer tabs** = STATIC faithful **1920×1080** frames (`buildRemotionFramePreview`) CSS-scaled into a 16:9 box (`.prep-frame`/`.prep-frame__stage`) by a shared `ResizeObserver` (`scale = clientWidth/1920`), every px/colour copied VERBATIM from each runner's `___Remotion___/<n>_…_Remotion/src/scenes/Level.tsx` + the shared `PlayerSlot` + reveal panel. Replicates the **3D-perspective pitch** (`perspective(1200)` + surface `rotateX(38deg) translateY(-11%) scale(1.0925)`) with the 11 player cards at their formation slot `x/y%`, depth `sizeComp(slot.y)` ported from `slotZ`. **Runner 1:** QUESTION = nationality-flag circle fronts; ANSWER reveal panel = club crest 296 → team name → divider → flag. **Runner 2 (national, added 2026-06-14, `previewPlayerCard`/`previewRevealPanel`):** QUESTION = the player's **CLUB CREST** floating directly on the pitch (`crestImgPct` 72%, `objectFit:contain`, `drop-shadow(0 6px 14px …)`, NO white disc) via `resolveSlotClubCrestUrlsForLevel` (NEW export in `pitch-render.js` — per-slot override → canonical league file → `(1) Other Teams` chain, same as `renderSlot`); ANSWER = photo+red-name-band back cards (`playerPhotoPathsForLevel` + `pitchSlotDisplayLabel`, both now exported) + the **NationalRevealPanel** slid in left: **national-team LOGO** 296 (`getHeaderLogoUrlChain(lvl,cs,"national",…,"nat-by-club")` → `National Team Logos/<name>.png`) → name (56) → divider → country flag 232×188; if NO logo file, falls back to the no-logo layout (flag 296×216 on top + name 58). Both: pitch shifted +150px right on ANSWER, level badge(`ordinal`) + timer (3 on Q, 0 faded on A). XI via `levelXi` (per-level `customXi` else `pickStartingXI`, non-mutating). STATIC only; the "Revealed" toggle does NOT affect these frames (only the editable Assets grid). Runner 2 token `?v=20260614-prep2tabs`.
    - **Player-name parity + BLACK name (runner 1, 2026-06-17):** the faithful ANSWER frame's red name band used the RAW `pitchLabelFromPlayerName(player.name)`, so a NAME-cube edit (e.g. "ALEXANDER-ARNOLD"→"ARNOLD") showed on the editable Assets card but NOT in the Answer preview. Fixed: `pitchSlotDisplayLabel` is now **exported** from `pitch-render.js` (and hardened to `state?.customNames?.[…]`) and `previewPlayerSlot` uses `pitchSlotDisplayLabel(lvl, player)`. The rendered VIDEO had the same gap — `build-data.mjs` `displayName()` only applied the PERMANENT `player_name_overrides_shared.json`, ignoring per-save `customNames`; now `display = lvl.customNames[p.name] || displayName(p.name)` so the **per-save** NAME edit ("only for THIS save", which `captureCurrentScriptObject` already serializes as `level.customNames`) renders too. **Name COLOUR is now BLACK everywhere** (was black on the editable Assets card = pitch.css `.slot-name #111111`, but WHITE+8-offset-black-outline in the Answer frame and the video): runner 1's LOCAL `___Remotion___/1_…_Remotion/src/components/PlayerSlot.tsx` band + the `previewPlayerSlot` band both → `color:#111111`, `textShadow:none`. Runner 1 has its OWN local `components/PlayerSlot.tsx` (Level.tsx imports `../components/PlayerSlot`, NOT `@shared`), so this is runner-1-only — the shared `.remotion-shared` PlayerSlot (runners 3–9) is untouched. Reload Remotion Studio (HMR) for the component; the build-data `customNames` change applies on the next data rebuild.
- **Auto-save (2026-06-12):** every prep-panel edit fires `prep:dirty` and `save-picker.js` debounces a write of `captureCurrentScriptObject()` into **`block.script`** via the `replace` op (the manual "Save to save" button was removed). Remotion `build-data.mjs` reads `block.script.levels`, so edits reach the rendered video automatically; photos/crests are disk files under `Images/` and are picked up immediately. The panel's `run_site.py` also schedules a **debounced (3s) `node scripts/build-data.mjs`** after every data-mutating POST so `src/generated/{saves,audio}.json` + the shared public sync refresh automatically (open Remotion Studio hot-reloads). **Server must be restarted once to activate.**
- **Kept tools:** full Voice tab, Update Data, Name & Description, PROD validation, bulk photo fetch, photo source picker/crop, saved team layouts, BGM 5-song preview (in the voice tab).
- **Slimmed shims:** `js/levels.js` (`switchLevel` just moves the pointer + fires `prep:level-switched`) and `js/transitions.js` (only `transitionSettings` + capture/apply + the Look-tab select) keep the script-object schema byte-compatible.
- All importers of `saved-scripts.js`/`levels.js` use the per-runner prep token (`?v=20260612-prep` / `-prep2`, split-brain rule).

## Runner 3 Regular = Remotion prep panel (2026-06-13, CUSTOM career-path layout)

`3_Guess The Player By Carrer Path - Main Runner - Regular` was converted to a prep panel like runners 1/2, but with a **custom per-level layout** (single-player-per-level career quiz, not an 11-card lineup). Scaffolding mirrors runner 2 (save-picker `3|` blocks, slim `levels.js`/`transitions.js`, build-data hook in `run_site.py` → `___Remotion___/3_…_Remotion`, blob module loader + `_try_delete_team_logo`, deletions of `video/progress/obs-recorder/recording-flow/recording-preflight/recording-queue/thumbnail-studio.js` + `html/landing|logo|outro|progress.html` + `css/thumbnail-studio.css`). Token `?v=20260613-prep3`.

**Why the layout is custom:** `renderCareer()` (pitch-render.js) targets a SINGLE `#career-wrap` by id and owns many globals, so it is NOT cloned/retargeted per section the way runners 1/2 swap `els.pitchSlots`. Instead `js/prep-panel.js` builds the layout itself and re-uses runner 3's DATA resolvers + edit ENDPOINTS. `header.html` (#team-header) + `career.html` (#career-wrap) stay loaded but HIDDEN so `renderHeader()`/`renderCareer()` don't crash.

**Each `.prep-section` body = a 3-TAB block (`buildLevelView`/`buildLevelTabs`, default tab = Assets, 2026-06-14):** `Assets` · `Questions` · `Answer`; switching just toggles `pane.hidden`.
- **ASSETS tab (`buildAssetsPane`) — all the EDITABLE content (unchanged editing):** a row of [the player photo box (`buildPlayerBoxes`) + a **Flag** card (`resolvePlayerStatsNationalityFlagUrl(player.nationality)`, now exported from `pitch-render.js` — flagcdn `w320`, England = repo asset) + a **Data** card (Player / Nationality / Clubs)], then BELOW it the existing editable **career path** (`buildCareerPath`: the cleaned `state.careerHistory` clubs as crest boxes with `→` arrows + `+` add-team picker; missing crest = COPYABLE name box; each box **LOGO** `/__team-logo/fetch` + **X** `/__team-logo/delete`). The player box still has **PHOTO** (`/__ready-photo/from-url`) + **X** (`/__ready-photo/delete`) + **RB** (remove bg) + **✎ NAME**. Every edit calls `markPrepDirty()` and rebuilds the section so the Questions/Answer frames refresh.
- **QUESTIONS / ANSWER tabs (`buildRemotionFramePreview`, state `"question"`/`"answer"`):** a STATIC, faithful **1920×1080 stage** (`.prep-frame__stage`, every px/colour copied VERBATIM from `___Remotion___/3_…/src/scenes/Level.tsx`) CSS-scaled into a 16:9 box (`.prep-frame`) by a shared `ResizeObserver` (`scale = clientWidth/1920`). Shows: the **career-path crest ROW** at top:52% (frosted-glass cards `careerCrestSize(n)` + gold-chevron arrow discs + year labels; on ANSWER it dims to opacity 0.5 + blur 13px + scale 0.97), the **level badge** (156px gold disc) + **timer** (162px ring, mid number on Question, 0 on Answer), the **bottom-anchored player** (`previewPlayer`: QUESTION = theme-tinted black silhouette + cool-blue glow/backlight, z12 behind the row; ANSWER = colour Ready photo + drop-shadow, z26 in front), and on ANSWER the **name banner** (white first-name(s) 64px + gold surname 150px). No animation — both end states only.

**Player-name overrides (NEW for runner 3):** added the shared bucket runners 1/2 use — `getPlayerNameOverride`/`setPlayerNameOverride`/`initPlayerNameOverridesSharedSync` in `pitch-render.js` POST to `/__runner-json-blob/player_name_overrides_shared` (keyed by canonical `careerPlayer.name`), and runner 3's Remotion `build-data.mjs` now reads `player_name_overrides_shared.json` via `playerDisplay()`. **FULL name (2026-06-14):** `playerDisplay = override || FULL name` (NOT `displayName()` which returns only the surname), and the reveal `NameReveal`/`NameBanner` (runners 3 + 4) use `display` first → the complete name shows (white first-name + gold surname). Cleared 2 leftover test overrides (`Senesi→SENESI`, `Trent Alexander-Arnold→ARNOLD`) that were forcing surnames. The blob module (`dev_server_runner_blob.py`) is now loaded in run_site.py. **Server must be restarted once to activate the build-data hook + blob module.** **OVERRIDE SCOPED OUT of 3 & 4 (2026-06-17, per user):** runners 3 & 4 now ALWAYS show the player's full real name — `playerDisplay = (name) => name` in their `build-data.mjs` (no longer reads the shared bucket) and `displayPlayerNameFor()` returns `playerNameFor()` with NO `getPlayerNameOverride`; the **✎ NAME button was removed** from runners 3 & 4's player box. The rename/override feature is now scoped to the lineup runners **1 & 2 only** (their cards need short names). A stale per-browser localStorage override (`career-stats-shared:player-name-overrides:v1`) is now simply ignored.

## Runner 4 Regular = Remotion prep panel (2026-06-14, career-STATS layout)

`4_Guess The Player By Carrer Stats - Regular` was converted to a prep panel like runner 3, reusing runner 3's prep scaffolding (`prep-panel.js`/`save-picker.js`/`prep-panel.css` copied + adapted). **Lighter conversion than runner 3:** the play/record JS files were NOT deleted — they remain on disk but inert (`app.js` keeps the imports, but the `initFloatingEmojis()`/`initRecordingQueue()`/`initThumbnailStudio()` calls were removed, the quiz-type handler calls `renderPrepPanel()` instead of `renderRecordingQueue()`, `initPrepPanel()`+`initSavePicker()`+`initPlayerNameOverridesSharedSync()` are added at the end of `init()`, and the one UNGUARDED `els.playVideoBtn.onclick=…` was wrapped in `if (els.playVideoBtn)` so the removed Play button can't crash init). `index.html` is a slim prep version (`#prep-root`, NO play/record/landing/outro/progress, styles `?v=20260614-prep4`); `header.html`+`career.html` stay loaded-but-hidden. Save-picker reads `4|` blocks; its `saved-scripts.js` import token is aligned to `?v=20260601-autoopen5` (matches app.js — avoids the token split-brain). **LEFT side = a FAITHFUL Remotion frame preview (`buildRemotionFramePreview` in `prep-panel.js`, 2026-06-15):** a fixed **1920×1080 stage** (`.prep-frame__stage`, every px/colour copied VERBATIM from `___Remotion___/4_…/src/scenes/Level.tsx`) CSS-scaled into a 16:9 box (`.prep-frame`) by a shared `ResizeObserver` (`scale = clientWidth/1920`) — shows the level EXACTLY as it renders: the 3-box info cluster (Position/Games · CLUBS 2× · Goals/Assists, neutral-glass `StatCard`/`ClubsCard` styling, crests max-4-per-row), the level badge + timer, and the **per-level country flag as a STATIC waving cloth** (`previewWavingFlag` = the same strip/skew maths as `WavingFlag` frozen at design frame 8; flag URL via `resolvePlayerStatsNationalityFlagUrl(player.nationality)`, now exported from `pitch-render.js`), and the **REVEALED player** in its true spot — `previewHeroPlayer` mirrors `HeroPlayer` at `revealProgress=1` (colour Ready photo, bottom-anchored & centred, fixed `PLAYER_H`=690 so all are the same height, grown ×1.09, colour filter + ground shadow), in front of the flag (z30) and behind the info boxes (z40). The RIGHT box still holds the editable player photo + controls. The editable clubs grid (add/remove/year/LOGO) is kept BELOW the preview (`Clubs — edit`); remove/add rebuild the section so the preview updates (a freshly-fetched LOGO shows in the preview after the next render/reload). Body is now `align-items:flex-start`; left column capped `max-width:56rem`. Styles token bumped `?v=20260615-prep4preview`.

**Each `.prep-section` body (flex row):**
- **LEFT — career-stats info (`buildCareerStatsInfo`):** a 2×2 grid of READ-ONLY stat cards — **GAMES** (`formatPlayerCareerTotalStat(player,"appearances")`), **POSITION** (`formatPlayerPositionLabel`), **GOALS** / **GOALS CONCEDED** (GK), **ASSISTS** / **CLEAN SHEETS** (GK) — values summed from `careerPlayer.club_career_totals` + `national_team_career_totals` (all 3 stat fns now `export`ed from `pitch-render.js`); then **ALL** the career **CLUBS** as a NO-ORDER wrap-grid of editable crest boxes (`buildClubsGrid`/`allCareerClubs` over the full `careerHistory`, NO arrows/`+` — `buildCrestBox` reused for LOGO/X). Stat numbers are not editable (auto from the player's data); to fix them, fix the player's Transfermarkt data.
- **RIGHT — one player box:** identical to runner 3 (`buildPlayerBoxes` — the 16:9 bottom-anchored photo + PHOTO/X/RB/✎ NAME).

**Prep helpers ported into runner 4 `pitch-render.js`** (they didn't exist here): `markPrepDirty`, `resolveCareerPlayerPhotoUrlForPrep` (wraps the existing `pickLoadableReadyPhotoUrlForVariant`), the shared player-name-override block (`get/setPlayerNameOverride`, `initPlayerNameOverridesSharedSync` → same `/__runner-json-blob/player_name_overrides_shared` bucket), and `resolveCareerClubLogoUrls`. `saved-scripts.js` already had `applyScriptObject`/`buildScriptFromImportText`; `photo-helpers.js` already had `getClubLogoOtherTeamsRelPath`. **`run_site.py` hooks DONE (2026-06-14):** added `import subprocess`, the `dev_server_runner_blob.py` loader (`_runner_blob_mod`, wired into `do_GET`/`do_POST` for `/__runner-json-blob/…` so the ✎ NAME override persists to the SHARED `player_name_overrides_shared.json` → cross-runner), and the debounced `schedule_remotion_build_data()` (REMOTION_PROJECT_DIR → `4_…_Remotion`) fired after the recording-status + blob POSTs. Since EVERY prep edit calls `markPrepDirty()` → auto-saves `block.script` via recording-status, the build-data rebuild covers all edits. Runner 4's Remotion `build-data.mjs` now uses `playerDisplay = (name) => name` (the FULL real name; the shared override is NO LONGER applied — scoped to runners 1 & 2 only, per user 2026-06-17), and the ✎ NAME button was removed from runner 4's player box. **Server must be restarted once to activate the blob module + build-data hook.** Photo box: identical to runner 3 (16:9, Ready-photo only) — a player with no Ready photo shows "no photo" until PHOTO/RB adds one (not a bug).

## A runner's `js/` modules
**Core/state:** `app.js` (entry/wiring), `state.js` (`appState`), `constants.js`, `dom-bindings.js` (caches DOM into `appState.els`), `ui-panels.js`, `bootstrap-hybrid.js`, `dev-live-reload-state.js`.
**Teams/levels:** `levels.js` (`switchLevel`), `teams.js` (search/load squad), `formations.js` (slot coords per formation), `pick-xi.js`, `progress.js`, `level-control.js`, `nationality-pool-key.js`, `search-normalize.js`, `custom-selects.js`.
**Render/visuals:** `pitch-render.js` (`renderPitch`/`renderHeader`, slot flips), `transitions.js` (GSAP overlays), `team-header-hatch.js`, `flag-stripe-colors.js`, `emojis.js`, `thumbnail-studio.js`. (The "QUESTIONS + BONUS" pill style picker `landing-qstyle-switcher.js` was removed from runner 1 Regular on 2026-06-09 — only the ticket-stub look remains, baked into `landing.css`; other runners may still have it. See [video-record-render.md](video-record-render.md).)
**Photos:** `photo-helpers.js`, `photo-source-picker.js`, `bulk-photo-picker.js`, `photo-crop.js` (see [images.md](images.md)).
**Video/audio:** `video.js` (`startVideoFlow`/`runVideoStep`), `audio.js` (voices + BGM), `bgm-crossfade-preview.js`, `team-voice-manager.js`, `bundled-level-voices.js`, `voice-tab.js`.
**Record/render:** `recording-flow.js`, `recording-preflight.js`, `obs-recorder.js`, `recording-queue.js`, `render-mode.js`, `render-segments.js`, `render-progress-ui.js`, `render-test-clips-ui.js`, `prod-validation.js` (see [video-record-render.md](video-record-render.md)).
**Data/saves:** `saved-scripts.js`, `saved-team-layouts.js`, `update-data.js`, `runner-saved-server-sync.js`, `i18n.js`, `paths.js` (see [saves-and-data.md](saves-and-data.md)).
**`render/` (Node, headless):** `index.mjs`, `worker.mjs`, `lib.mjs`, `audio-mux.mjs`, `audio-filtergraph.mjs`, `segment-budgets.mjs`.

## Shared modules (`.Storage/shared/`)
`image-cache.js`, `asset-probe.js`, `team-image-paths.js`, `prod-asset-validation.js`, `record-language-chooser.js`, `recording-preflight-core.js`, `video-status.js`, `ball-preloader-animation.js`, `ball-merge-soccer-clones.js`, `backgrounds/background-theme.js`, `debug-overlay.js`, `loading-overlay.js` (DISABLED — no-op per user), `modal-layer.js`, `shorts-intro-quiz-title-fit.js`, `block-import-text.js`, `import-pair-format.js`, `import-player-manual-clubs.js`, `update-data-freshness.js`, `name-description-generator/` (see [content-generation.md](content-generation.md)).

## `appState` (state.js)
Single source of truth. Holds `els` (60+ DOM refs), `teamsIndex {clubs, nationalities}`, `levelsData[]` (index 0 logo, 1 landing, 2..N questions, last = outro), `currentLevelIndex`, `totalLevelsCount`, `playerImages`, `flagcodes`, session fields (`currentSquad`, `selectedEntry`, `squadType`, `formationId`, `displayMode`), video/record flags (`isVideoPlaying`, `rendering`, `videoMode`, `doubleRecording`), `bundledVoiceVariants`, `bgmSongs`. Helpers: `getState()`, `getQuizQuestionCount()`, `initLevels()`.

## Cache-busting (important)
Every JS/CSS import carries a `?v=` token. `app.js` is injected with `?v=<Date.now()>` (always fresh); CSS uses a semantic token in `index.html` (e.g. `styles.css?v=20260604-renderintro`).
- **CSS edit not showing** → bump the `styles.css?v=` token in `index.html`.
- **JS module edit not showing** → bump the `?v=` in the importer.
- **Split-brain bug**: if two importers load the *same* module with *different* `?v=` tokens, the browser creates **two module instances** with separate state. Symptom: "only the tab I reloaded works" / "Load a saved setting first". Fix: align tokens across all importers (app.js, recording-queue.js, saved-scripts.js, render-mode.js).
- Prefer **one `Write` per file** over many edits — rapid live-reload saves stall the page.

## Pitch player cards (Guess Team Name — Regular)

Runner **`1_Guess The Football Team Name - Main Runner - Regular`** renders each XI slot as a **portrait trading card**: white rounded frame, player photo, **red name band** (18% of card height) with **black italic uppercase text** at fixed size (`font-size: 13.2cqh` on `.slot-name`). Card footprint uses `--slot-card-size: 0.9` on `.player-slot`. Video flip **back** uses the trading-card frame; club-quiz video **front** (`.slot-front--club-flag`) is flag-only at old circle size via `.slot-inner--flag-flip` (scale → rotateY morph, 0.88s ease). Styles in `css/components/pitch.css`; DOM in `renderSlot`. Bump `styles.css?v=` in `index.html` after CSS edits.

## i18n
`i18n.js` `t(key)` over `TRANSLATIONS.{english,spanish}` + `translateCountry()`. `voice-tab.js` **forces language back to english on every page load** (record phase 2 left it spanish); runtime switches via `setCurrentLanguage()` still work. Shorts intro title is language-aware (`SHORTS_INTRO_QUIZ_TITLE_*`).

## Remotion projects — all Regular runners (`___Remotion___/`)

Every **Regular** runner (1–9) has a [Remotion](https://www.remotion.dev/) project that re-creates its **play-video** look as a deterministic React composition (separate from the browser play/record/render pipeline). They all live under the top-level **`___Remotion___/`** folder:

```
___Remotion___/
├─ Open Remotion.bat            ← opens ALL projects in ONE Studio (Compositions list)
├─ _studio/                     ← aggregator: registers every runner's composition
├─ Ready Videos/<Quiz Name>/<Language>/<save>.mp4   ← render output tree
├─ 1_…Team Name…_Remotion/      ← each project: + its own "Open Remotion Studio.bat"
├─ 2_…National Team…_Remotion/
└─ … 3–9 …
```

### Shared architecture (the important part)
- **Shared code library: `.remotion-shared/src/`** (repo root, committed; a sibling of the gitignored `.remotion-shared/public/` asset cache). Holds the pieces that are **byte-identical for every runner**: `scenes/BallIntro.tsx` (4-ball merge intro), `scenes/Outro.tsx` (the ending/CTA), `scenes/Intro.tsx` (landing **shell** — title/season/badge are props), `effects/*` (all background colors/effects/competitions), `transitions/*`, `components/Stage|SoccerBall|Pitch|PlayerSlot|RevealPanel`, `timing.ts`, `theme.ts`, `paths.ts`, `ending.ts`, `scene-props.ts`, and `build-lib.mjs` (shared build-data helpers).
- Projects import shared code via the **`@shared/*` alias** (configured per project in `remotion.config.ts` `resolve.alias` + `tsconfig.json` `paths`), so the folder nesting doesn't matter. `remotion.config.ts` also adds the project's `node_modules` to `resolve.modules` (so shared files outside the project root resolve react/remotion) and sets `setPublicDir` to `../../.remotion-shared/public` (one shared asset cache for all).
- **A per-runner project is only ~4 bespoke things:** `src/config.ts` (title strings EN/ES, theme default, `COMPOSITION_ID`), `src/scenes/Level.tsx` (+ any reveal components — the ONLY runner-specific UI), `src/level-data.ts` (resolves the save → display data), and `scripts/build-data.mjs` (its data source). Everything else (`FootballQuizDemo.tsx` scene-wiring + audio + duration math, `Root.tsx`, `schema.ts`, `index.ts`, `scripts/render.mjs`) is copied from runner 2 and only re-pointed at the local files. **Runner 1 is the original self-contained project (it predates the shared lib and keeps its own copies) — leave it as the visual reference.**

### Data + build
`scripts/build-data.mjs` imports `../../../.remotion-shared/src/build-lib.mjs` (helpers: `repoPaths`, `buildPhotoIndex`, `buildClubCrestIndex`, `buildFlagResolver`, `buildSquadPlayerIndex`→`findPlayer(club,name)`, `makeVoiceHelpers(VOICES_SRC, revealDir)`, `buildAudioManifest`, `firstBgm`, `syncAssets`, `syncVoices`, `COMMON_ASSETS`, `REVEAL_EN/ES`). It reads the runner's `recording-status.json` blocks (key prefix `N|`, **named blocks only**), writes `src/generated/{saves,audio}.json`, and syncs referenced assets into `.remotion-shared/public/`. **Data sourcing differs by runner:** runners 2–8 store only a `teamsImportText` list (the squad/team data is rebuilt at build time) — `"Team - Continent"` (r2 national teams), `"Team - Country"` (r7 crests), `"PlayerName - Club"` (r3/4/5/6/8 → `findPlayer` loads the player's full record from the club squad JSON in `Squad Formation/Teams`). **Runner 9 (MCQ) embeds the full `mcq` object per level** (question + A/B/C answers + `correctAnswerId` + `topicImage`) — no `teamsImportText`.

### The runners
| # | Composition id | Level (question → reveal) | Data clue | Theme default |
|---|---|---|---|---|
| 1 | `Guess-The-Football-Team-Name-Regular` | 11-slot pitch, nationality flags → player photos; club crest + flag panel | save's embedded XI | Club by Nationality |
| 2 | `Guess-The-Football-National-Team-Regular` | 11-slot pitch, **club crests** → photos; **national flag + name** panel | national squad XI | `#1B5E20 - National Team by Club` |
| 3 | `Guess-The-Player-By-Career-Path-Regular` | **career path** (broadcast layout, like "guess the player by their transfers"): player BOTTOM-ANCHORED & centred (cropped at frame bottom + ground-shadow ellipse, never floating), hidden as a dark THEME-TINTED silhouette: a black base `<Img>` for the shape + rim glow, tinted by a gradient painted into the player shape via the cutout photo's alpha as a CSS `maskImage` (softer + more dimensional than flat black). The tint + rim glow + backlight colours are ALL derived from the active background/competition colour (`silhouetteColorsFor(bg.colorHex)` → a dark same-hue gradient that's always darker than the bg + a lighter same-hue glow), so it looks right on ANY theme (navy on blue CL, dark green on Ligue 1, etc.) — NOT hardcoded blue. Lit by a soft cool BACKLIGHT (clean rim, not a flat halo). **Reveal = a DROP-AND-POP**: the player drops straight DOWN fully OFF the bottom of the screen (`swoopY`→`OFFSCREEN`≈1220px), swaps silhouette→colour WHILE hidden (so colour pops out), then POPS back up from the bottom and lands IN FRONT of the logos (clean translateY with a soft settle — NO scale squash, per user). A z-index flip (player `zIndex` 12→26, crest row =20) happens while off-screen (`inFront` at revF≥15) so the behind→front swap is unseen; backlight + ground shadow fade out while off-screen (`onFloor` from `swoopY`). The whole motion is FAST (~20 design frames): a sharp V-trough — `Easing.in` ACCELERATE down off-screen by `DOWN`=8f, then `Easing.out` IMMEDIATELY rebound up, finishing by `UP_END`=20f. NO flat hold at the bottom (no empty-screen pause) and no overshoot. The NAME slams in (`NAME_DELAY`≈18f) as it pops back in front. Clue = a single row of BIG WHITE crest cards joined by dark arrow-discs (gold chevron, centred on the crest via `height:size`), year under each, positioned over the body at `top:59%`; crest size SCALES with club count 1..14 via `careerCrestSize()` (cap 78..300px); each card has a phase-staggered up/down BOB (+tilt) during the question (ripples like a wave). On reveal the row stays VISIBLE but BLURS into a soft frosted backdrop behind the player (`rowBlur` 0→13px, opacity only 1→0.5 — never disappears) starting RIGHT AT the reveal tick (`revF` 0→10) in sync with the player drop — NOT after he returns — and the NAME slams across the bottom (white first-name + gold `COLORS.accent` surname, 150px). **Photo sizing = SAME HEIGHT for all (like runner 4):** the portrait box is a fixed `PLAYER_H`(1044px, ~ the old "97%") and BOTH the silhouette + colour `<Img>` share `PLAYER_IMG_STYLE` = `height:100%; width:auto; object-fit:contain; object-position:center bottom; display:block` so every player is scaled to the SAME height (small photos UP, tall ones down; width free so never squashed), and since the reveal is pure translateY (NO scale-grow) the hidden silhouette and the revealed colour are ALWAYS the identical height. Any remaining apparent-size difference between players is the source photo's own crop (tight headshot vs full body), inherent to the Ready photos, same limitation as runner 4. Photo = the no-bg Ready photo (`buildReadyPhotoIndex`/`resolvePlayerPhoto`). Rebuilt 2026-06-13, see `scenes/Level.tsx` | player `transfer_history` (or per-save edited `careerHistory` from block.script) | `#0069EC - Career Path` |
| 4 | `Guess-The-Player-By-Career-Stats-Regular` | **rebuilt 2026-06-14 `Level.tsx`** — distinct from runner 3: a **3-column info cluster across the TOP** — POSITION/GAMES stacked LEFT, a big **CLUBS** box (2× — spans both rows) CENTRE, GOALS-or-CONCEDED/ASSISTS-or-CLEAN-SHEETS stacked RIGHT (drop-in spring). The CLUBS card shows **max 4 crests per row** (`rows=ceil(n/4)`, balanced cols), crests sized **as big as the cell allows** (cap 140px), a big **hidden player** centred & bottom-anchored as a glossy BLACK silhouette with a white/blue RIM-LIGHT (so it pops), and the player's **nationality flag WAVING behind** them. The flag is a SMOOTH CSS-transform cloth (`WavingFlag`, `FLAG_STRIPS=70`): vertical strips each offset by a travelling sine (`translateY`) AND **skewed by that wave's local slope** (`skewY`), OVERLAPPING by 1px each side so there are no seams. Fold-shading is GEOMETRY-ONLY strips + a SINGLE travelling soft-light gradient overlay (sinusoidal light/dark, period `2π/K·width`, `backgroundPositionX` advanced by the wave) — NOT per-strip `brightness()` (that caused clearly-visible vertical bands; removed). CRITICAL: this uses ONLY CSS transforms, NOT SVG filters — `feTurbulence`/`feDisplacementMap` rasterise COARSELY in the Studio preview (looked "very pixelated" to the user even though full renders were fine); CSS transforms stay crisp at any preview zoom. (Two earlier versions — plain strips, then SVG displacement — were both rejected as pixelated.) NO three.js — Remotion can't run the browser's WebGL `player-stats-flag-three.js`. Plus a `Flagpole` + diagonal sheen. **Reveal (≠ runner 3's drop-and-pop):** an IN-PLACE colour materialise — the colour photo wipes UP from the feet via a `maskImage` linear-gradient while the silhouette fades, with a scale-pop + golden glow, a `RevealBurst` (radial white flash + conic light-rays + shock ring), the flag billows harder, and a `NameBanner` (white first-name + gold last-name) slams up. Boxes fade out on reveal (`uiOpacity`); 128px badge/timer + a wide CLUBS box, row centred to clear the corners. The **CLUBS card scales crest size to the count** (1 row ≤4, 2 rows 5–12, 3 rows 13+; `crestSize=min(cellW,cellH)` capped 74). Player photo = the no-background **Ready photo** (`buildReadyPhotoIndex` in build-data, same as runner 3 — NOT the raw `buildPhotoIndex` club image, which was the bug), falling back to the club image. The HeroPlayer container is FULL-WIDTH + `justifyContent:center` (not shrink-to-fit `left:50%`) so the player is dead-centre. **Photo sizing = SAME HEIGHT for all (per user):** `imgStyle = height:100%; width:auto` so every player photo is scaled to EXACTLY `PLAYER_H`(690) regardless of aspect (small photos scaled UP, tall ones down; width free → never squashed). Same style on the silhouette + colour layers (+ the `pop` grow on the shared container) so hidden and revealed always match. `PLAYER_H` 690 chosen so the ×1.09 reveal-grow still clears the top cluster (bottom ≈312). (History: a `maxWidth:640` cap made wide players shorter = inconsistent; a width-normalise pass was then replaced by this height-normalise per the user.) **Crests:** build-data prefers the prep's edited `block.script…careerHistory` (cleaned/deduped — matches the prep) over raw `transfer_history`, filters youth/reserve (`isJunkClub`), and resolves crests via `resolveClubCrest` → `resolveClubCrestBest` (a best-effort fallback that replicates the browser's scoring + breaks ambiguous ties by COUNTRY prominence, e.g. "Al-Ittihad" → Saudi Arabia not Libya — fixes the "?" the strict index left). Remaining "?" = obscure clubs with no crest file in `Images/Teams`. | player `club_career_totals` + Ready photo + `countryFlagPath` | `#AB47BC - Career Stats` |
| 5 | `Guess-The-Player-By-Club-Position-Country-Age-Regular` | **2×2 frosted cards** (club crest, position, country flag, age) + portrait → photo + name | player record | `#5C6BC0 - Club + Position + Country + Age` |
| 6 | `Guess-The-Fake-Information-Regular` | same 4-param grid but ONE card is **fake** → flips to real value on reveal (fake-stat voice) | player record + deterministic fake | `#E57373 - Fake Information` |
| 7 | `Guess-The-Football-Team-Logo-Name-Regular` | ONE big crest **obscured** (blur+dark) → clear crest + team name | team crest | `#81C784 - Football Team Name` |
| 8 | `Guess-The-Football-Player-Name-Regular` | ONE player photo **silhouetted** → full color + name | player photo | `#9575CD - Football Player Name` |
| 9 | `Football-Quiz-Multiple-Choice-Regular` | question + **A/B/C cards** (trivia: topic image + text; which-player: 3 photo cards) → correct highlighted green | embedded `mcq` | `#C2185B - Football Quiz (Multiple Choice)` |

All share the **same intro, outro/ending, background effects, transitions, level/countdown handling, and EN/ES language** by construction (one shared source). The `Level` component signature is uniform: `<Level bg={…} level={lvl} levelNumber={i+1} language={…} />`, with `REVEAL_START = 185` design frames (the flip/reveal tick) and the shared gold `LevelBadge` (top-left) + countdown `Timer` ring (top-right) that fade out on reveal.

**Per-competition BRAND ACCENT (`@shared/brand-accent.ts` `brandAccent(competitionLabel)`):** so the intro/title/bonus aren't all gold — each competition background gets a distinct curated accent = the rough COMPLEMENT ("negative") of that bg's dominant colour so it pops (`ACCENT_BY_LABEL`: CL silver-white/blue, Europa cyan/orange, Conference pink/green, PL teal/magenta, La Liga gold/red, Bundesliga white/red, Serie A amber-gold/blue, Ligue 1 violet/lime, World Cup sky-blue/gold, Euro coral/navy, Generic 1 cyan/crimson, Generic 2 aqua/brown, Generic 3 lime/slate, Generic 4 orange/steel-blue). Verified by rendering one intro still per competition and checking each accent contrasts its bg. Returns `{main, light, dark, ring (conic), linear, glow(a)}`. Shared `UltimateIntro` + `Intro` (title screen) + `BonusIntro` each take an optional `accent` prop (default gold). WIRED into ALL 9 runners: each `FootballQuizDemo` computes `const accent = brandAccent(props.competition)` (the `competition` enum value matches the `ACCENT_BY_LABEL` keys exactly) right after `resolveBackground(props)` and passes it to `<UltimateIntro/>`, `<Intro/>`, `<BonusIntro/>`. Recolors: emblem ring/glow/rays + FOOTBALL QUIZ name + underline (UltimateIntro); SEASON kicker + chips/number-disc + title glow (shared Intro); starburst + "BONUS QUESTION!" text (BonusIntro). GOTCHA fixed in rollout: shared `BonusIntro` "BONUS" text used `background-clip:text` (Remotion hide-bug) → switched to solid `accent.main`.

**SINGLE-SOURCE INTRO/TITLE/BONUS (all 9 identical except the Level):** the title screen `Intro` is the redesigned **kicker + hero title + chips** ("N QUESTIONS" dark-glass pill + "+ BONUS" pill) with a gentle up/down bob, parameterized by each runner's `INTRO_STRINGS`/`TITLE_FONT_SIZE`(155)/`SEASON_FONT_SIZE`(72) from its `src/config.ts`. Runner 1 NO LONGER has local `Intro`/`BonusIntro` — it imports both from `@shared/scenes/*` like runners 2-9 (its `config.ts` now holds the strings). Every Remotion runner pulls intro/title/bonus/ending/outro(think-you-know break)/background/transitions from `@shared`; the ONLY per-runner files are `Level.tsx` (+ `schema.ts`, `level-data.*`, `config.ts`, generated `audio.json`). Runner 1 still keeps byte-identical local copies of Ending/Outro/AnimatedBackground/effects-data/iris/ending.ts/transitions (legacy; kept in sync — they render the same output).

**Branded "Ultimate Football Quiz" intro — ALL 9 runners (`@shared/scenes/UltimateIntro`, `@shared/transitions/intro-handoff`, `@shared/components/SoccerBallClean`, now SHARED).** Each runner's `FootballQuizDemo.tsx` has `const USE_ULTIMATE_INTRO = true` + the helper block (`GREETING_START_F`, `GREETING_PORTION_SEC`, `SPEECH_END_SEC`, `greetingPortionFrames`, `effectiveIntroFrames`, `introTitleFrames`, `TITLE_TAIL_FRAMES`) with PER-RUNNER timings measured from that runner's combined voice clip. Each quiz type has its OWN single combined intro voice (greeting + that quiz's title) at `.Storage/Voices/Intro Greeting/<key>/<lang>/intro.mp3` (keys: teamname, nationalteam, careerpath[=base welcome.mp3], careerstats, fourparams, fake, teamlogo, playername, mcq), resolved in each `build-data.mjs` → `audio.introGreeting`. Set `USE_ULTIMATE_INTRO=false` per runner to revert to `BallIntro`. (Original runner-3-only writeup below still describes the mechanics.) Same duration as `BALL_INTRO_FRAMES` (92 design frames ≈ 3.07s) so it's a DROP-IN swap with no timing changes — gated by `const USE_ULTIMATE_INTRO = true` in `FootballQuizDemo.tsx` (set `false` to revert to `BallIntro`, which is kept). Look: sits on the SAME bright competition bg as the rest of the video (NO full-screen dark overlay — that caused a bright→dark→bright flicker at the start/hand-off; branding pops via strong element shadows/outlines instead) + rotating gold stadium-light rays, a gold conic-ring emblem with the shared `SoccerBall` (spins + back-out drop-in), then `NAME_TOP`/`NAME_BIG` slam in (white track-in + big gold-gradient name with a moving shine sweep — a white band on a duplicate text layer clipped via `background-clip:text`, NOT a container `overflow:hidden` which would clip the drop-shadow into a "box" — + drawn underline) and sparkles drift; settles into the iris hand-off to the quiz title. Edit `NAME_TOP`/`NAME_BIG` to retune the channel name. ~3s chosen per YouTube retention best-practice (brand lands without viewers bailing). **Intro voice = ONE continuous clip (greeting + quiz title):** a single ElevenLabs generation per language, e.g. EN "Hey everyone, welcome to the Ultimate Football Quiz! Let's get started. Guess the player by career path." (ES equivalent). NOT two stitched clips — user wants one long sound. Files `.Storage/Voices/Intro Greeting/<lang>/` (welcome.mp3 / bienvenidos.mp3), resolved in `build-data.mjs` → `audio.introGreeting`. In `FootballQuizDemo.tsx` it plays from `GREETING_START`(4); the separate quiz-title voice is SKIPPED when `useGreeting` (it's inside the clip). The ball opens at the PAUSE between the greeting and "Guess…" part: `GREETING_PORTION_SEC` (~4.05s, measured via `ffmpeg silencedetect`) drives `effectiveIntroFrames = GREETING_START_F + greetingPortionFrames + IRIS_FRAMES`, so the title reveal syncs with "Guess the player…". The quiz-title screen ends + level 1 transitions in EXACTLY when the spoken line ends: `SPEECH_END_SEC` (trailing-silence start, measured per language) drives `introTitleFrames = ceil(SPEECH_END_SEC*30) - greetingPortionFrames + transFrames` (overrides `introFramesForLanguage` on the Ultimate path) — no lingering delay after the voice. Regenerate with voice id `yl2ZDV1…`, model `eleven_v3`; re-measure BOTH the pause (`GREETING_PORTION_SEC`) and the speech end (`SPEECH_END_SEC`) via `ffmpeg silencedetect` if the line changes. (Each quiz type should get its own single combined clip with its title.) The intro→quiz-title hand-off uses a custom `transitions/intro-handoff.tsx` `introHandoff()` — the SOCCER BALL is the switch element: it STARTS at the intro emblem ball's exact position+size (`EMBLEM_Y`≈−151px above centre, `START_SCALE`=196/420) so it's the SAME ball continuing (not a new one popping up mid-screen), lifts to centre + zooms up to fully COVER the frame, then OPENS — an iris hole (`radial-gradient` mask on the ball) grows from its centre while the ball keeps expanding — revealing the quiz title behind it (like the original ball intro). It opens & reveals, never shrinks/closes back. The expansion is ONE continuous curve (`Easing.out(quad)` over the whole transition, no two-phase split) so the ball never freezes/pauses when the iris starts — both run together. No spin. The ball is `components/SoccerBallClean.tsx` — the supplied CSS ball: 400px white sphera + dark ring + 5 dark border-triangle `PATCH_BASE` patches + a single `clip-path` central pentagon, scaled to any `diameter` (native 412px × `diameter/412`). The 5 seams are radial `<svg><line>`s (`SEAMS`) from the pentagon vertices. GOTCHA: the border-triangle patches need `boxSizing:'content-box'` (a global `border-box` collapses them). NOTE: a fully-symmetric SVG rebuild was tried and REVERTED — the user preferred this CSS-patch look; nudge the `SEAMS` coords for small line tweaks, do NOT rebuild the ball. The intro's `FOOTBALL QUIZ` shine is a white-band gradient LAYERED in the same `background-clip:text` element as the gold (a separate clipped layer rendered as a box). (Earlier SVG re-draws of the ball were rejected; this CSS port is what the user wanted.) — runner 3's emblem + transition both use it (the hand-tuned shared CSS `SoccerBall` had a lopsided central pentagon that showed badly in the close-up zoom; shared one still used by other runners' `BallIntro`). The intro fades out behind the growing ball; the title is full by the time the iris starts (~p0.45), so the two text lock-ups are NEVER on screen together (no text-into-text morph — user rejected cross-fade AND masked-wipe variants for that reason). Reuses the shared `SoccerBall`; no shine/flash. ONLY on the `USE_ULTIMATE_INTRO` path; the original `iris()` opening still runs when the toggle is off. Reuses `IRIS_FRAMES` (39) so no timing math changed.

**Ending voice (mirrors Regular play video):** two clips per language (`think-you-know` / `how-many`) from `.Storage/Voices/Ending Guess/`; chosen by the **Ending** prop. **Random** resolves once per save via `@shared/ending` `resolveEndingKey()` (deterministic hash) so voice + `Outro.tsx` title stay matched. The voice `<Sequence>` starts **0.5s after** `outroStart` (`ENDING_VOICE_DELAY_SEC` in `FootballQuizDemo.tsx`). Outro scene length is `outroFramesForEnding()` (voice duration + ~1s tail).

### Run / Studio / render
- **One Studio for everything:** double-click `___Remotion___/Open Remotion.bat` (runs `remotion studio ../_studio/studio-entry.tsx` from runner 2's folder, reusing its install) → Compositions panel lists all 9. Per-project: each folder's `Open Remotion Studio.bat` (or `npm run studio`).
- **"Save default props" in the aggregated Studio:** the studio server resolves the file it rewrites via Remotion's `<stem>-entry.tsx` → `<Pascal>Root.tsx` convention (`getRootFileFromEntryPoint` in `@remotion/studio-server`). That is why the entry is named `studio-entry.tsx` and the root `StudioRoot.tsx` — with any other names the server falls back to `src/Root.tsx` **of the cwd (runner 2)**, whose `id={COMPOSITION_ID}` variable defeats static extraction → "Could not find or extract defaultProps". Each `<Composition>` in `StudioRoot.tsx` must also keep a literal string `id` and a fully inline `defaultProps` literal (`as const` is fine; no spreads/variable refs). The same applies to every per-runner `src/Root.tsx` (used by each folder's own `Open Remotion Studio.bat`): all 9 now hardcode the id (= config.ts `COMPOSITION_ID`) and inline color/effect (= config.ts `THEME_DEFAULT`) — keep them in sync by hand if config.ts changes.
- **Render to the Ready Videos tree:** `npm run render -- --save "<Save>" --language English [--levels All]` → `Ready Videos/<Quiz Name>/<Language>/<save>.mp4` (via `scripts/render.mjs`). Verified end-to-end (1920×1080 H.264+AAC) for runners 2 + 9.
- Re-run `npm run build-data` after the save list / voices / assets change (it also updates `quizTitleDurationSec` / `endingDurationSec` used for scene lengths).

### Competition background themes (Remotion) — the ONLY background control
The `color` + `effect` props were REMOVED from all 9 Remotion schemas (2026-06-12); `competition` alone now defines the whole background. Two kinds of entries in `COMPETITIONS`:
- **Real competitions** — gradient + a UNIQUE sparse pattern (5 small shapes per 300px tile, alpha ≈0.05, slow downward drift — the CL stars set the subtlety benchmark): CL=5-point stars, Premier League=crown outlines (faint cyan), La Liga=rings+dots, Ligue 1=hexagon outlines, Euro=4-point sparkles, Bundesliga=diamond (Raute) outlines, Serie A=scudetto shield outlines, World Cup=center rays, Europa/Conference League=diagonal stripes.
- **Generic 1–4** — fresh gradients (none of the old 16 quiz colors) + ONE animated effect each via `recipe.effectId` (`pattern: "none"`): 1=Sun spiral/crimson, 2=Sun rays top-right/bronze, 3=Rising question marks/charcoal, 4=Rising soccer balls/steel-blue. (Started as 10; user culled to these 4 on 2026-06-12.) All background motion runs at ONE global speed — `DRIFT_PX_PER_SEC` in `AnimatedBackground.tsx` (rotation periods + sprite travel are derived from it so the *visible* speed matches the CL stars drift).

Recipes + SVG tile generators in `.remotion-shared/src/effects/effects-data.ts` (`COMPETITIONS`, `*TileUri()`); rendering in `AnimatedBackground.tsx` (`CompetitionBg` + effect overlay when `recipe.effectId` is set). NOTE: runner 1 keeps LOCAL COPIES of both files (`1_…/src/effects/`) — copy the shared versions over them after editing shared. Each runner's `resolveBackground` lives in its `schema.ts`.

### Known gaps (non-blocking, by-design or asset-dependent)
- **Reveal voices** are missing for many teams/players (TTS not yet generated) — the Level just skips the voice (no crash); generate them in the browser runners' voice tab, then re-run `build-data`.
- **Career-history crests** (runner 3/4 clubs grid) use short transfer_history names (e.g. "Dortmund", "Birmingham") that don't always match `Images/Teams/*.png` filenames → some crest slots are blank. Same limitation as the browser runner; an alias map could improve it.
- Runner-9 MCQ quiz-title voice + per-answer voices mostly absent (no `Game name/Football Quiz MCQ` clips yet).
